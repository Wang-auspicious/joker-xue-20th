# Joker Xue's 20th

This repository contains the packaged interactive HTML project and the minimal source/data/assets needed to rebuild it.

## Open directly
Open `index.html` in a browser.

## Included
- `index.html` final deliverable
- `src/` React scene source
- `build-react-index.cjs` build script
- `generated-data.json`, `generated-dormancy.json`, `data.local.js`, `raw-scene-signals.json`
- `uploads/` cover art assets
- `moxp0x09-薛之谦---尘.mp3`, `moxp0vxi-薛之谦---尘.flac`, `尘.lrc`
